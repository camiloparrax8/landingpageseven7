Esta carpeta se rellena con `npm run sync-assets` (o automáticamente antes de `dev` / `build`).

Origen: carpeta `assets/` en la raíz del repositorio (al mismo nivel que `seven7-next/`).

Archivos requeridos por el diseño:
- seven7-logo.png
- seven7-banner.jpg

Rutas en la app: ver `lib/site-assets.ts` y clases `.hero-media` / `.page-hero` en `app/globals.css`.
